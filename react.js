/**
 * ESLint rule react
 *
 * eslint-plugin-react@7.4.0
 */

module.exports = {
  extends: [
    './index.js',
  ],
  plugins: [
    'react'
  ],
  settings: {
    react: {
      pragma: 'React', // React 的指令名称，默认为 'React'
      version: '16.0' // React 的版本号，默认为最新的稳定版
    }
  },
  rules: {








  }
}